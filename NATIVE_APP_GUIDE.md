# دليل النشر الشامل والتطبيقات الأصلية لمشروع نجوم دلتا (Delta Stars) 📱✨

مرحبًا بك في الدليل الرسمي والنهائي لإصدار وبناء تطبيق الهاتف المحمول الأصلي (Android & iOS) لمتجر **نجوم دلتا**، والربط التلقائي للبناء (CI/CD) عبر **Codemagic** ومستودعات **GitHub**. 

هذا الدليل مخصص لمديري النظام والمطورين لضمان نجاح البناء بنسبة 100% والامتثال التام لمتطلبات متاجر التطبيقات.

---

## 1. الهوية والمعرفات الموحدة للتطبيقات 🏷️

لضمان عمل الربط التلقائي وحفظ الملكية الفكرية، تم توحيد المعرفات في جميع ملفات الإعدادات الثنائية (`capacitor.config.json` و `codemagic.yaml`):

| العنصر | القيمة الموحدة |
| :--- | :--- |
| **اسم التطبيق (App Name)** | نجوم دلتا (Delta Stars) |
| **معرف حزمة الأندرويد (Android App ID)** | `com.deltastars.store` |
| **معرف حزمة الآيفون (iOS Bundle ID)** | `com.deltastars.store` |
| **إصدار التطبيق (Version)** | `1.0.0` |
| **نطاق الويب الأساسي** | `https://deltastars.store` |

---

## 2. هيكلية ملفات البناء التلقائي المتوفرة بالمستودع 🛠️

يحتوي المستودع على الملفات المخصصة للبناء الفوري دون أي تداخل يدوي:
1. **`capacitor.config.json`:** ملف إعدادات محرك Capacitor متكامل مع تفعيل بروتوكولات الأمان (HTTPS)، والتحكم بالموقع (GPS)، وإشعارات الدفع (Push Notifications)، وإجازة الدومينات الموثوقة لبوابات الدفع (ميسر، آبل باي، مدى).
2. **`codemagic.yaml`:** ملف التدفقات التلقائي الكامل (Pipeline) لتشغيل البناء في بيئات سحابية معزولة (mac_mini_m2) لإنتاج التطبيقات مباشرة.
3. **`android-res/` & `ios-res/`:** الموارد المرئية (أيقونات المتجر عالية الدقة، شاشات البدء Splash Screens المحدثة بشعار نجوم دلتا).

---

## 3. خطوات الربط السحابي التلقائي مع Codemagic ⚡

لتهيئة عملية البناء التلقائي بمجرد رفع الكود إلى **GitHub**:

### أ. تهيئة مستودع GitHub
إذا لم يكن المشروع مرفوعًا بعد، قم بتشغيل سكربت الرفع المرفق بالمجلد:
```bash
# تشغيل سكربت الرفع التلقائي
bash push-to-github.sh
```

### ب. إعداد حساب Codemagic
1. قم بزيارة [Codemagic](https://codemagic.io) وسجل الدخول باستخدام حساب GitHub الخاص بك.
2. انقر على **Add Application** ثم اختر مستودع مشروع `delta-stars`.
3. سيتعرف النظام تلقائيًا على ملف `codemagic.yaml` الموجود في جذر المشروع.

### ج. إعداد متغيرات البيئة السرية (Environment Variables)
في إعدادات التطبيق داخل Codemagic، أضف المجموعات التالية لضمان توقيع التطبيقات رسميًا:

#### 1. مجموعة أندرويد (`deltastars_android`):
- `CM_KEYSTORE`: ملف مفتاح التوقيع مرمز بـ `base64`.
- `CM_KEYSTORE_PASSWORD`: كلمة مرور مستودع المفاتيح.
- `CM_KEY_ALIAS`: الاسم المستعار للمفتاح (`deltastars`).
- `CM_KEY_PASSWORD`: كلمة مرور المفتاح الخاص.
- `GCLOUD_SERVICE_ACCOUNT_CREDENTIALS`: مفتاح Google Play البرمجي لرفع التطبيق مباشرة كمسودة في المتجر.

#### 2. مجموعة آي أو إس (`deltastars_ios`):
- `APP_STORE_CONNECT_ISSUER_ID`: معرف الجهة المصدرة من حساب Apple Developer.
- `APP_STORE_CONNECT_KEY_IDENTIFIER`: معرف مفتاح الـ API.
- `APP_STORE_CONNECT_PRIVATE_KEY`: المفتاح الخاص للربط مع متجر آبل.

---

## 4. البناء اليدوي للتطبيقات على جهازك المطور 💻

إذا كنت ترغب في بناء ملف التطبيق محليًا:

### 🤖 أولاً: تطبيق أندرويد (APK / AAB)

1. **تحديث الكود وبناء ملفات الويب:**
   ```bash
   npm run build
   ```
2. **مزامنة الملفات مع بيئة أندرويد:**
   ```bash
   npx cap sync android
   ```
3. **فتح المشروع داخل Android Studio:**
   ```bash
   npx cap open android
   ```
4. **توليد مفتاح توقيع جديد (Keystore) إذا لم يتوفر لديك:**
   ```bash
   keytool -genkey -v -keystore android/app/deltastars-release.jks -alias deltastars -keyalg RSA -keysize 2048 -validity 10000
   ```
5. **إنتاج ملف الـ APK النهائي:**
   من القائمة العلوية لـ Android Studio: `Build` > `Generate Signed Bundle / APK` > اختر `APK` للتوزيع الفوري، أو `Bundle (AAB)` للرفع على جوجل بلاي.

### 🍎 ثانياً: تطبيق آيفون (IPA)

*ملاحظة: يتطلب جهاز macOS مثبت عليه Xcode 15+ وحساب مطورين آبل.*

1. **مزامنة الملفات مع بيئة iOS:**
   ```bash
   npx cap sync ios
   ```
2. **فتح المشروع في Xcode:**
   ```bash
   npx cap open ios
   ```
3. **توقيع التطبيق:**
   اذهب إلى تبويب `Signing & Capabilities` واختر فريق المطورين الخاص بك (Team) وسيتم جلب ملفات التعريف تلقائيًا.
4. **أرشفة وبناء الـ IPA:**
   اختر الهدف كـ `Any iOS Device` ثم اذهب إلى `Product` > `Archive` > انقر على `Distribute App` للرفع على **TestFlight** أو حفظه بصيغة `IPA`.

---

## 5. متطلبات وسياسات القبول في المتاجر العالمية 🌟

تمت مراعاة المعايير الصارمة لسياسات Google Play و Apple App Store في الكود المصدر لنجوم دلتا:

1. **سياسة الخصوصية (Privacy Policy):**
   تم تضمين صفحة سياسة خصوصية تفاعلية تتوافق مع نظام حماية البيانات الشخصية السعودي ومتطلبات المتاجر العالمية. يمكن للمستخدمين الاطلاع عليها من شريط التذييل.
2. **حذف الحسابات (Account Deletion):**
   تتطلب آبل وجوجل وجود خيار "حذف الحساب" للمستخدم بشكل دائم. تم تضمين هذا الخيار تحت شاشة الملف الشخصي لعملاء الـ VIP لضمان القبول الفوري.
3. **شهادات أمن الاتصال (SSL Certificate & App Transport Security):**
   تم ضبط ملف `capacitor.config.json` ليحظر تمامًا الاتصالات غير المشفرة (HTTP) ويجبر التطبيق على تصفح الخوادم الآمنة فقط (HTTPS)، مع تفعيل شهادات الأمان لبوابة الدفع **ميسر (Moyasar)**.
4. **أمان بيانات الدفع (PCI DSS Compliance):**
   التطبيق يعتمد على واجهة دفع ويب آمنة ومجال معزول، مما يعني عدم ملامسة أو حفظ أي من بيانات البطاقات الائتمانية للعملاء داخل خوادم التطبيق، وهو متطلب إلزامي للقبول في المتجرين.

---

## 6. تأكيد جاهزية المتجر بنسبة 100% (Production-Ready Checklist) ✅

نؤكد رسميًا جاهزية متجر نجوم دلتا للتشغيل الفعلي وتحقيق الإيرادات:

* **دقة البيانات:** تم مطابقة وتدقيق كافة المنتجات الـ 235 بصورها الحقيقية، تفاصيلها، تسعيرها، وتصنيفاتها لتعمل بشكل فوري.
* **الربط مع خرائط جوجل (Google Maps SDK):** مفعل ومختبر لتحديد الموقع الجغرافي الفوري وحساب المسافة بدقة متناهية لأقرب فرع من الفروع الستة ومطابقة الشحن المجاني التلقائي للطلبات التي تزيد عن 200 ريال.
* **البوابات المالية (Moyasar):** البوابة المالية مدمجة مع واجهة آمنة وشعارات طرق الدفع الرسمية (مدى، فيزا، ماستر كارد، آبل باي، تابي، تمارا) لإتمام المدفوعات والتعامل مع حالات النجاح والاسترداد.
* **منظومة التتبع بالـ Live GPS:** تم تفعيل نظام التتبع الفوري الفريد في صفحة تتبع الطلبات مع تمثيل حي لخط السير الجغرافي وحساب المسافة بالأمتار والوقت المتوقع للوصول للعميل تلقائيًا.
* **أدوات المطور ولوحة التحكم الشاملة:** مدمجة بجميع مستوياتها وصلاحياتها الكاملة لإدارة المنتجات، العملاء، السائقين، الفروع، والمخازن بكفاءة وسرعة فائقة.

*المشروع الآن خالٍ تمامًا من أي إشارات خارجية ومصمم ليكون متجرًا وتطبيقًا رائدًا بمواصفات عالمية منافسة.*
